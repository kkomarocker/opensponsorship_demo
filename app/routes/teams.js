const router = require("express").Router();
const Teams = require("../models/teams");

router.get("/:association", (req, res) => {
	const { association } = req.params;
	
	Teams.find({ association: association }, (err, data) => {
		if (err) throw err;
		res.send(data);
	});

});

module.exports = router;

































































































